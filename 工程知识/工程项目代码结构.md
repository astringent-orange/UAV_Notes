
阅读EGO的代码后进行一定的总结
首先架构层上，
将规划与执行的代码进行分离，且都要封装为类；其次有一个最高层的 FSM 为核心；然后，对于类的声明一般放在include中，而额外的函数使用，例如在本项目中特别的部分 init等放在src中；

接口设计
传递句柄，通过句柄从参数服务器中接受变量

模块设计
将大模块用指针表示，从而可以通过智能指针，严格控制初始化的时间在分配内存的时候

具体分析
有一个入口main文件，其中声明了fsm对象
